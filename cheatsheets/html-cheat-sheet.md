# 📄 HTML Cheat Sheet (The Skeleton)

HTML is used to define the **structure** of your page.

### 🏠 Common Tags
- `<div>`: A container (box) for other elements.
- `<h1>` to `<h6>`: Headings (1 is the biggest).
- `<p>`: A paragraph of text.
- `<span>`: Small piece of text inside a line.
- `<img>`: Display an image.
- `<button>`: Something you can click.
- `<a>`: A link to another page.

### 🔑 Key Attributes
- `id="unique-name"`: Give an element a unique ID so JS can find it.
- `class="style-name"`: Give an element a class name so CSS can style it.
- `src="path/to/image.jpg"`: The source of an image.

```html
<div id="player-box" class="card">
  <h1>Player 1</h1>
  <p>Score: 0</p>
  <button id="btn-rock">Rock 🪨</button>
</div>
```
