<script setup>
import { ref } from 'vue'
// 1. Import SoundTile and BaseButton here

// 2. Define the 'tiles' array with IDs, labels, and colors

// 3. Define reactive state (score, timeLeft, isPlaying, currentTargetId)

// 4. Create the startGame logic

// 5. Create the handlePlay logic (What happens when a tile is clicked?)

</script>

<template>
  <div class="min-h-screen flex flex-col items-center justify-center p-6">
    <!-- 6. Build the Header (Title, Score, Timer) -->

    <!-- 7. Build the 3x3 Grid (v-for through tiles) -->

    <!-- 8. Build the Game Over / Start Overlay -->
  </div>
</template>
